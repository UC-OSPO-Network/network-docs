# Quarto render output
*_files/
*.html
!index.html
# Rendered presentation decks are published to the site. Keep them self-contained
# (embed-resources: true) so no per-deck _files directory is needed.
!presentations/**/*.html
.quarto/
_site/

# OS / editor
.DS_Store

/.quarto/
**/*.quarto_ipynb
__pycache__/
